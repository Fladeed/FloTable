/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        unoptimized: true
    },
    output: 'export',
    trailingSlash: true,
    reactStrictMode: true,
}

export default nextConfig